/**
 * Used for root html tag only
 */
export const htmlLanguage = "cz"
/**
 * Used to determine serve locale. Centralized here to make global changes easier.
 */
export const serverLocale = "cz"

export const serverTimeZone = "Europe/Prague"
